How to play:
*Host:
  -Multiplaying --> HostGame --> waiting for opponent
*Join:
  -If you're in the same internet, just only typing the other's IP
  -Otherwise, install RadminVPN, join in the same network, typing the IP producted by it 
